{
  "name": "prisma_database",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "devBuild:build": "npx prisma migrate dev --create-only --name init",
    "devBuild:generate": "npx prisma generate",
    "devBuild:studio": "npx prisma studio",
    "devBuild:serve": "npx prisma studio",
    "devBuild:run": "npx prisma studio",
    "devBuild:build:run": "npx prisma migrate dev --create-only --name init && npx prisma generate && npx prisma studio",
    "devBuild": "npx prisma migrate dev --create-only --name init && npx prisma generate",
    "devStart": "nodemon script.ts",
    "devRun": "nodemon script.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.13.0",
    "nodemon": "^2.0.15",
    "prisma": "^4.10.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@prisma/client": "^4.10.1",
    "express": "^4.18.2"
  }
}
